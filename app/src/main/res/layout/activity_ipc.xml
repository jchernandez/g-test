<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View"/>
        <variable
            name="viewModel"
            type="com.rojoxpress.gbmtest.ui.IPCViewModel" />
    </data>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp">

                <com.github.mikephil.charting.charts.LineChart
                    android:id="@+id/line_chart"
                    android:layout_width="match_parent"
                    android:layout_height="250dp"
                    android:visibility="@{viewModel.ipc != null ? View.VISIBLE:View.INVISIBLE, default=gone}"/>

                <ProgressBar
                    android:id="@+id/pb_chart"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:visibility="@{viewModel.ipc == null ? View.VISIBLE:View.GONE}"/>

            </FrameLayout>


            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:orientation="vertical">

                <ProgressBar
                    android:id="@+id/pb_lists"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:visibility="@{(viewModel.ipc != null &amp;&amp; viewModel.top == null) ? View.VISIBLE:View.GONE, default=gone}"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@color/black"
                    android:textColor="@color/white"
                    android:text="@string/rises"
                    android:padding="5dp"
                    android:visibility="@{viewModel.top != null ? View.VISIBLE:View.GONE, default=gone}"/>

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rv_rises"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@color/black"
                    android:textColor="@color/white"
                    android:text="@string/falls"
                    android:padding="5dp"
                    android:visibility="@{viewModel.top != null ? View.VISIBLE:View.GONE, default=gone}"/>

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rv_falls"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@color/black"
                    android:textColor="@color/white"
                    android:text="@string/volume"
                    android:padding="5dp"
                    android:visibility="@{viewModel.top != null ? View.VISIBLE:View.GONE, default=gone}"/>

                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/rv_volume"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />
            </LinearLayout>


        </LinearLayout>

    </androidx.core.widget.NestedScrollView>
</layout>